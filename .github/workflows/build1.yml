name: Build TALIH-PD2 Kernel with KernelSU (Ubuntu 22.04 Final)

on:
  workflow_dispatch:
    inputs:
      ksu_tag:
        description: 'KernelSU tag (4.19 必须 ≤ v0.5.2)'
        default: 'v0.5.2'
        type: string
      defconfig:
        description: 'Defconfig 名（不填则自动查找 mt8797 开头）'
        default: ''
        type: string

env:
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: read

    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # ── 1. 合并 KernelSU ──
      - name: Setup KernelSU
        run: |
          curl -LSs https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh | bash -s ${{ inputs.ksu_tag }}

      # ── 2. 安装 GCC 9 交叉编译器（Ubuntu 22.04 源）──
      - name: Install GCC 9 cross compiler
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            gcc-9-aarch64-linux-gnu g++-9-aarch64-linux-gnu \
            bc flex bison libssl-dev make git cpio python3

      # ── 3. 确定 defconfig ──
      - name: Detect defconfig
        id: def
        run: |
          if [ -n "${{ inputs.defconfig }}" ]; then
            DEF="${{ inputs.defconfig }}"
          else
            DEF=$(ls arch/arm64/configs/ | grep -im1 'mt8797.*_defconfig' | head -1)
            if [ -z "$DEF" ]; then
              echo "::error::找不到 mt8797 defconfig，请手动填写"
              exit 1
            fi
          fi
          echo "name=$DEF" >> $GITHUB_OUTPUT
          echo "Using defconfig: $DEF"

      # ── 4. 配置内核（开启 KSU / kprobes / overlayfs）──
      - name: Configure kernel
        run: |
          export ARCH=arm64 SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CC=aarch64-linux-gnu-gcc-9

          make O=out ${{ steps.def.outputs.name }}

          ./scripts/config --file out/.config \
            -e KPROBES -e HAVE_KPROBES \
            -e OVERLAY_FS \
            -e KSU
          make O=out ARCH=arm64 olddefconfig

      # ── 5. 编译内核 ──
      - name: Build Image.gz-dtb
        run: |
          export ARCH=arm64 SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CC=aarch64-linux-gnu-gcc-9

          make O=out -j$(nproc) Image.gz-dtb 2>&1 | tee build.log
        continue-on-error: true

      # ── 6. 检查编译产物 ──
      - name: Check build result
        run: |
          if [ -f out/arch/arm64/boot/Image.gz-dtb ]; then
            echo "✅ Image.gz-dtb 生成成功"
            ls -lh out/arch/arm64/boot/Image.gz-dtb
          else
            echo "::warning::Image.gz-dtb 未生成，查看 build.log"
            tail -200 build.log
          fi

      # ── 7. 打包 AnyKernel3 ──
      - name: Package AnyKernel3
        if: always()
        run: |
          if [ ! -f out/arch/arm64/boot/Image.gz-dtb ]; then
            echo "::notice::无 Image.gz-dtb，跳过打包"
            exit 0
          fi
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 -b master ak3
          cp out/arch/arm64/boot/Image.gz-dtb ak3/
          cd ak3
          zip -r ../TALIH-PD2-KSU-${{ inputs.ksu_tag }}-final.zip *
          echo "AnyKernel3 刷机包已生成"

      # ── 8. 上传编译产物 ──
      - name: Upload Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: TALIH-PD2-KSU-${{ inputs.ksu_tag }}-final
          path: |
            TALIH-PD2-KSU-*-final.zip
            out/arch/arm64/boot/Image.gz-dtb
            build.log
